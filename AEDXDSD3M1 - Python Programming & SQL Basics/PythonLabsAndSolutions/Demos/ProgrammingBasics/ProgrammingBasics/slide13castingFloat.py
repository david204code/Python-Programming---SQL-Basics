price = int(input('What is the price? '))  # only allows integer values, crashes when using decimals

totalPrice = price * 1.2                   # automatically casts an integer to a float.

print('Total Price is',totalPrice)

price = float(input('What is the price? '))

totalPrice = price * 1.2

print('Total Price is',totalPrice)

