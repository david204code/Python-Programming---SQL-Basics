
names = ["Bob", "Steve", "Helen"]

for name in names:
    print(name)
    # add the string '01' to each of the name
    #name += '01'
    #print(name)

#for name in names:
    #print(name)

#modify the list
#change the content of the list so we need to access, so we are access the elements and change
#the value in that list.
#for x in range(len(names)):
#sub script so the actual content of the list is modify
#    names[x] += '01'

#print(names)
